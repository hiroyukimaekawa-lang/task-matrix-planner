# 📅 タスクマトリクス プランナー — 社内デプロイ・ユーザー利用マニュアル

本システムは、重要度と期日ベースでタスクの優先順位を「10点満点」で可視化し、各自のGoogleカレンダーに直接双方向同期できるマルチユーザー対応の社内タスク管理アプリケーションです。

本マニュアルには、**「管理者向けの公開・セキュリティ設定手順」**と、**「一般ユーザー向けの利用ガイド」**を記載しています。

---

## 🛠️ Part 1. 管理者向け：社内デプロイ・公開設定手順

本システムは、サーバー代完全無料（**コスト0円**）で強固なセキュリティを担保した状態で社内に公開できます。

### 1. Google OAuth の公開制限の解除（重要）
現在、Googleログインおよびカレンダー連携は「テストモード」になっています（テストユーザーに登録されたアドレスしかログインできません）。社内の誰でも各自のアカウントでログインできるように、以下の公開設定を行ってください。

1. **[Google Cloud Console (OAuth 同意画面)](https://console.cloud.google.com/apis/credentials/consent)** にアクセスします。
2. 左上で今回のプロジェクト `task-matrix-38c69` が選択されていることを確認します。
3. 画面中央の「公開ステータス」にある **「アプリを公開 (PUBLISH APP)」** ボタンをクリックします。
4. ステータスが「本番環境（In Production）」に切り替われば完了です！
   * *※これにより、社内の全社員が自分のGoogleアカウントで自由にログイン・連携できるようになります。*

### 2. Firestore のセキュリティルールの設定（本番仕様）
初期設定の「テストモード（誰でも自由にデータを見られる状態）」から、**「自分専用のタスクしか絶対に読み書きできない本番用セキュリティ」** にアップグレードします。

1. **[Firebase Console](https://console.firebase.google.com/)** にアクセスし、プロジェクトを開きます。
2. 左メニューの **「Firestore Database」** を選択します。
3. 画面上部の **「ルール (Rules)」** タブをクリックします。
4. エディタの中身を以下のコードに書き換えて、右上の **「公開 (Publish)」** をクリックします。

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /tasks/{taskId} {
      // ログインしているユーザー自身に紐づくタスクのみ、読み書き・削除を許可する強固なセキュリティ
      allow read, write: if request.auth != null && request.auth.uid == resource.data.userId;
      allow create: if request.auth != null && request.auth.uid == request.resource.data.userId;
    }
  }
}
```
* *※この設定により、同一データベースを使用しながら、他人のタスクは管理者であっても一切見られなくなる「完全プライベート」な空間が担保されます。*

### 3. Cloudflare Pages での社内公開デプロイ
1. GitHub リポジトリ（`hiroyukimaekawa-lang/task-matrix-planner`）を Cloudflare Pages に接続します。
2. ビルド設定は以下のように指定するだけで、自動でデプロイが行われます。
   * **フレームワーク プリセット**: `Vite` (または `なし`)
   * **ビルドコマンド**: `npm run build`
   * **出力ディレクトリ**: `dist`
3. 接続情報はコード内に極めて安全な形でハードコードされているため、環境変数の設定 is 一切不要です！

---

## 👤 Part 2. ユーザー向け：タスクマトリクス利用マニュアル

社内ユーザーへ周知・LP（ランディングページ）へ掲載するマニュアル用のテキストです。

### 1. ログイン方法と必須のアクセス許可（初回のみ）
1. 社内公開URL（例: `https://task-matrix-planner.pages.dev/`）にアクセスします。
2. ロマン溢れるログイン画面中央の **「Google アカウントでサインイン」** をクリックします。
3. ご自身のGoogleアカウント（または社用メールアドレス）を選択します。
4. **【超重要】** ログインの際、Googleから「追加のアクセス許可の要求」の画面が出ます。
   * **「Google カレンダーのすべての予定の表示、編集、共有、完全な削除」** のチェックボックスが画面に表示されますので、**【必ずON（チェック）にして】** から進めてください。
   * *※チェックを入れずに進めると、カレンダー同期ボタンを押した際に同期エラーが発生します。*

### 2. 優先度マトリクスと「10点満点スコア」の仕組み
本アプリは、単なるタスクリストではなく「やるべきタスクを直感的に視覚化する」システムです。

* **タスクの配置**:
  * 追加したタスクは、**「重要度（1〜5）」** と **「期日」** に応じて、2×2の4象限マトリクス（緊急かつ重要 / 重要だが緊急ではない / 緊急だが重要ではない / どちらでもない）に自動で配置されます。
* **10点満点の優先度スコア**:
  * 重要度と期日の近さに応じて、自動的に **`0.0` 〜 `10.0`** までの優先度スコアが算出されます。
  * スコアが高いもの（重大・高）かつ期日の近いタスクから順番に、下のリストで一番上に自動ソートされます。一番上のタスクから着手していくのが最も効率的です！

### 3. Googleカレンダー同期のやり方
日時をシステム上で決定するだけで、ご自身のカレンダーに反映できます。

1. カレンダーに登録したいタスクをクリックし、右側の詳細パネルを開きます。
2. **「期日の設定」**（カレンダーの日付）と **「時間の設定（30分刻みのプルダウン）」** を指定します。
   * *※時間を「設定なし」にした場合は、カレンダー上で「終日予定」として綺麗に配置されます。*
3. **「📅 Googleカレンダーに追加」** ボタンをクリックします。
4. カレンダーへ即時反映され、画面に「カレンダー連携済み」と表示されます！
5. **予定の変更も簡単**:
   * 日時やタスク名を変更して再度 **「📅 Googleカレンダーを更新」** をクリックすると、カレンダー側の既存の予定が上書き更新され、重複なくスケジュールが保たれます！
